import { useState } from "react";

function WorkShop() {
  const [upcomingMovies, setUpcomingMovies] = useState([]);

  return (
    <div>
      <h2>Workshop</h2>
    </div>
  );
}

export default WorkShop;
